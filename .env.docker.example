# Copy this file to .env.docker and replace the password before starting Compose.
# Use a URL-safe password because Compose builds CLINIC_DATABASE_URL from it.
POSTGRES_DB=clinic_booking
POSTGRES_USER=clinic_user
POSTGRES_PASSWORD=
CLINIC_TIMEZONE=Africa/Nairobi
CLINIC_JWT_SECRET=
